name: Test
description: Test all files and packages
author: Primo

runs:
  using: composite
  steps:
    - name: 🧪 Test
      id: Test
      shell: bash
      run: |
        pnpm run test \
          --cache-dir=".turbo" \
          --output-logs="errors-only"